import styled from '@emotion/styled';

export const IconStyled = styled.svg`
  width: 18px;
  height: 18px;
  display: inline-block;
  stroke-width: 0;
  stroke: currentColor;
  fill: transparent;
  :hover {
    stroke: currentColor;
  }
`;
